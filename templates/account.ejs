<% include head %>

<main>
  <h1>Account details</h1>
  <section class="center">
    <div class="row">
      <p><%= lang.account.username %>:</p>
      <p><%= userData.username %></p>
    </div>

    <div class="row">
      <p>E-mail adress:</p>
      <p><%= userData.email %></p>
    </div>
  </section>
</main>

<% include tail %>
